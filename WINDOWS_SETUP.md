# إعداد Windows لـ Flutter - Developer Mode

## 🚨 المشكلة:
```
Error: Building with plugins requires symlink support.
Please enable Developer Mode in your system settings.
```

## ✅ الحل:

### الخطوة 1: تفعيل Developer Mode

1. **افتح إعدادات Windows** (تم فتحها تلقائياً)
   - أو شغّل: `start ms-settings:developers`

2. **في صفحة "For developers"**:
   - ابحث عن **"Developer Mode"**
   - **فعّل** المفتاح (Toggle) بجانب "Developer Mode"

3. **سيطلب منك تأكيد**:
   - اضغط **"Yes"** للتأكيد

4. **انتظر حتى يتم التفعيل** (قد يستغرق بضع ثوانٍ)

### الخطوة 2: إعادة تشغيل التطبيق

بعد تفعيل Developer Mode:

```bash
flutter run
```

أو أعد تشغيل التطبيق من VS Code/Android Studio.

---

## 📋 شرح Developer Mode:

**ما هو Developer Mode؟**
- وضع مطور في Windows
- يُفعّل دعم symlinks (روابط رمزية) المطلوبة لـ Flutter plugins
- مطلوب لتطوير Flutter على Windows
- **آمن** - لا يؤثر على الأمان العام

**هل يحتاج صلاحيات Admin؟**
- لا، يمكن تفعيله بدون صلاحيات مدير
- لكن قد يطلب تأكيد من Windows Security

---

## ⚠️ إذا لم تظهر صفحة الإعدادات:

### الطريقة اليدوية:

1. **افتح Settings**:
   - اضغط `Windows + I`
   - أو ابحث عن "Settings" في قائمة Start

2. **ابحث عن "For developers"**:
   - في شريط البحث في Settings
   - أو اذهب إلى: **Privacy & security** → **For developers**

3. **فعّل Developer Mode**:
   - ابحث عن "Developer Mode"
   - فعّل التبديل

---

## ✅ بعد التفعيل:

### تحقق من التفعيل:

1. شغّل الأمر:
   ```bash
   flutter doctor
   ```

2. تأكد من عدم وجود أخطاء

3. شغّل التطبيق:
   ```bash
   flutter run
   ```

---

## 🎯 ملخص:

1. ✅ **فعّل Developer Mode** من إعدادات Windows
2. ✅ **انتظر التفعيل** (بضع ثوانٍ)
3. ✅ **أعد تشغيل التطبيق**

**بعد التفعيل، التطبيق سيعمل بشكل طبيعي!** 🚀

---

## 💡 ملاحظات إضافية:

- **Developer Mode مجاني** ولا يحتاج اشتراك
- **آمن** - فقط يُفعّل ميزات المطورين
- **يمكن إيقافه لاحقاً** إذا أردت
- **مطلوب مرة واحدة فقط** - بعد التفعيل سيعمل دائماً

---

**تم فتح إعدادات Windows تلقائياً. فعّل Developer Mode ثم أعد تشغيل التطبيق!** ✨


