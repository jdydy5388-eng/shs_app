# تشغيل Flutter على Android مباشرة

## ✅ الحل السريع:

حتى لو Flutter لا يرى الجهاز في `flutter devices`، يمكنك تشغيل التطبيق مباشرة على الجهاز باستخدام:

### الطريقة 1: استخدام device ID مباشرة

```bash
# 1. احصل على device ID
adb devices

# 2. شغّل التطبيق مباشرة
flutter run -d <device-id>
```

مثال:
```bash
flutter run -d 3a6bc15e
```

---

### الطريقة 2: استخدام adb مباشرة

```bash
# تأكد من أن الجهاز متصل
adb devices

# شغّل Flutter مباشرة
flutter run -d android
```

---

### الطريقة 3: إصلاح Flutter discovery (إذا استمرت المشكلة)

1. **أعد تشغيل Flutter daemon:**
   ```bash
   flutter clean
   flutter pub get
   ```

2. **أعد تشغيل Terminal/VS Code**

3. **جرب مرة أخرى:**
   ```bash
   flutter devices
   ```

---

## 📋 ملاحظات:

- ✅ **cmdline-tools** الآن في المكان الصحيح: `cmdline-tools\latest\bin\`
- ✅ **ADB** يعمل ويرى الجهاز
- ⚠️ **Flutter** قد يحتاج إعادة تشغيل للتعرف على الجهاز

---

## 🚀 بعد إصلاح cmdline-tools:

بعد نقل المجلدات إلى المكان الصحيح، يجب أن يعمل:

```bash
flutter devices
flutter run
```

---

**إذا استمرت المشكلة، استخدم `flutter run -d <device-id>` مباشرة!** 🚀

