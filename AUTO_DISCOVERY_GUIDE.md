# دليل الاكتشاف التلقائي للخادم

## ✅ ما تم إنجازه

تم إضافة نظام **اكتشاف تلقائي** لـ IP الخادم! الآن **لا حاجة لتعديل IP يدوياً** على كل جهاز.

## 🎯 كيف يعمل

### 1. على Windows (الخادم)
- ✅ يحاول الاتصال بـ `localhost` أولاً
- ✅ إذا نجح، يستخدم `localhost` تلقائياً
- ✅ يحفظ الاختيار لاستخدامه لاحقاً

### 2. على الجوال (أو أي جهاز آخر)
- ✅ يتحقق من IP محفوظ مسبقاً
- ✅ إذا لم يجد، يبحث في الشبكة المحلية تلقائياً
- ✅ يختبر IPs من `.1` إلى `.254` في نفس الشبكة
- ✅ عند العثور على الخادم، يحفظ IP تلقائياً

### 3. البحث الذكي
- ✅ يكتشف IP الجهاز الحالي تلقائياً
- ✅ يبحث في نفس الشبكة (أول 3 octets)
- ✅ يختبر IPs شائعة مثل `192.168.1.100`, `192.168.0.100`
- ✅ يحفظ IP المكتشف تلقائياً

## 📱 الاستخدام

### لا حاجة لأي إعداد يدوي!

1. **على Windows:**
   ```dart
   // لا حاجة لتغيير شيء - يعمل تلقائياً!
   static const bool useLocalMode = false;
   ```

2. **على الجوال:**
   ```dart
   // لا حاجة لتغيير شيء - يعمل تلقائياً!
   static const bool useLocalMode = false;
   ```

3. **شغّل التطبيق:**
   - سيكتشف الخادم تلقائياً
   - سيعمل مباشرة بدون أي تعديل!

## 🔧 للمستخدمين المتقدمين

### حفظ IP يدوياً (اختياري)

إذا أردت تحديد IP يدوياً:

```dart
await AppConfig.setServerIp('192.168.43.196', port: 8080);
```

### إعادة الاكتشاف

إذا أردت إعادة اكتشاف الخادم:

```dart
AppConfig.resetCache();
await ServerDiscoveryService.clearSavedServer();
```

## 📊 كيف يعمل الاكتشاف

```
1. التحقق من IP محفوظ
   ↓ (إذا وجد ومتاح)
   ✅ استخدامه
   
   ↓ (إذا لم يجد أو فشل)
   
2. على Windows/Linux/MacOS:
   - محاولة localhost
   - محاولة 127.0.0.1
   ↓ (إذا نجح)
   ✅ حفظ واستخدامه
   
   ↓ (إذا فشل)
   
3. البحث في الشبكة:
   - اكتشاف IP الجهاز الحالي
   - استخراج قاعدة الشبكة (مثل: 192.168.1)
   - اختبار IPs من .1 إلى .254
   ↓ (إذا وجد)
   ✅ حفظ واستخدامه
   
   ↓ (إذا فشل)
   
4. استخدام القيمة الافتراضية المحفوظة
```

## ⚠️ ملاحظات مهمة

1. **الاكتشاف الأول قد يستغرق بضع ثوانٍ** - هذا طبيعي
2. **بعد الاكتشاف الأول، سيتم حفظ IP** - سيصبح أسرع في المرات القادمة
3. **يجب أن يكون الخادم يعمل** قبل فتح التطبيق
4. **يجب أن تكون الأجهزة في نفس الشبكة WiFi**

## 🎉 المزايا

- ✅ **لا حاجة لتعديل IP يدوياً**
- ✅ **يعمل تلقائياً على جميع الأجهزة**
- ✅ **اكتشاف ذكي في الشبكة المحلية**
- ✅ **حفظ تلقائي للـ IP المكتشف**
- ✅ **سهولة الاستخدام**

## 🔍 استكشاف الأخطاء

### المشكلة: لا يجد الخادم

**الحل:**
1. تأكد أن الخادم يعمل
2. تأكد من أن الأجهزة في نفس الشبكة
3. يمكنك تحديد IP يدوياً:
   ```dart
   await AppConfig.setServerIp('192.168.43.196');
   ```

### المشكلة: بطء في الاكتشاف

**الحل:**
- الاكتشاف الأول قد يستغرق 10-30 ثانية
- بعد الاكتشاف الأول، سيتم حفظ IP وسيصبح فورياً

