# دليل الوضع المحلي - البدء بدون Firebase

## ✅ تم تفعيل الوضع المحلي!

يمكنك الآن البدء في التطوير والتجربة **بدون إعداد Firebase**! 🎉

---

## 🚀 البدء السريع:

### 1. تثبيت الحزم:

```bash
flutter pub get
```

### 2. تشغيل التطبيق:

```bash
flutter run
```

**هذا كل شيء!** التطبيق سيعمل محلياً باستخدام SQLite.

---

## ⚙️ إعدادات الوضع:

### التبديل بين الوضع المحلي و Firebase:

افتح `lib/config/app_config.dart`:

```dart
class AppConfig {
  static const bool useLocalMode = true;  // true = محلي
                                          // false = Firebase
}
```

- **`true`**: الوضع المحلي (SQLite) ✅ **الحالي**
- **`false`**: الوضع Firebase

---

## 📋 ما يعمل في الوضع المحلي:

### ✅ المميزات المتوفرة:

1. **✅ تسجيل الدخول وإنشاء الحسابات**
   - البريد الإلكتروني + كلمة المرور
   - المصادقة البيومترية (بصمة/وجه)

2. **✅ إنشاء المستخدمين**
   - مريض
   - طبيب
   - صيدلي

3. **✅ قاعدة بيانات محلية (SQLite)**
   - جميع الجداول جاهزة
   - البيانات تُحفظ محلياً على الجهاز

### ⚠️ ما لا يعمل (يحتاج Firebase):

1. **❌ الوصفات الطبية** - تحتاج خدمات إضافية
2. **❌ السجل الصحي** - يحتاج خدمات إضافية
3. **❌ الطلبات** - تحتاج خدمات إضافية
4. **❌ رفع الملفات** - يحتاج Firebase Storage
5. **❌ الإشعارات** - تحتاج Firebase Cloud Messaging
6. **❌ المزامنة بين الأجهزة** - تحتاج Firebase

---

## 🧪 اختبار التطبيق:

### 1. إنشاء حساب جديد:

1. شغّل التطبيق
2. اضغط "ليس لديك حساب؟ سجل الآن" (إذا كان موجوداً)
   - أو أنشئ حساب يدوياً من الكود

### 2. بيانات تجريبية:

يمكنك إنشاء حسابات تجريبية:

**مريض:**
```
البريد: patient@test.com
كلمة المرور: 123456
```

**طبيب:**
```
البريد: doctor@test.com
كلمة المرور: 123456
```

**صيدلي:**
```
البريد: pharmacist@test.com
كلمة المرور: 123456
```

---

## 📂 بنية قاعدة البيانات المحلية:

البيانات تُحفظ في:
- **Android**: `/data/data/com.example.shs_app/databases/shs_app.db`
- **iOS**: `Documents/shs_app.db`

**الجداول المتوفرة:**
- `users` - المستخدمون
- `prescriptions` - الوصفات
- `prescription_medications` - أدوية الوصفات
- `medical_records` - السجلات الطبية
- `orders` - الطلبات
- `order_items` - عناصر الطلبات
- `inventory` - المخزون

---

## 🔄 الانتقال إلى Firebase لاحقاً:

### عندما تريد استخدام Firebase:

1. **أكمل إعداد Firebase** (راجع `FIREBASE_COMPLETE_SETUP.md`)

2. **غيّر الإعدادات**:
   ```dart
   // في lib/config/app_config.dart
   static const bool useLocalMode = false; // تغيير إلى false
   ```

3. **أعد تشغيل التطبيق**

4. **البيانات المحلية لن تُنقل تلقائياً** - ستحتاج نقلها يدوياً إذا أردت

---

## 💡 نصائح للتطوير:

### 1. مسح البيانات المحلية:

إذا أردت مسح جميع البيانات:

```dart
final localDb = LocalDatabaseService();
await localDb.clearAllData();
```

### 2. تصفح قاعدة البيانات:

استخدم أداة مثل **DB Browser for SQLite**:
- حمّل قاعدة البيانات من مسار الجهاز
- استعرض الجداول والبيانات

### 3. البيانات التجريبية:

يمكنك إضافة بيانات تجريبية في الكود لاختبار الوظائف.

---

## ✅ الخطوات التالية:

### الآن يمكنك:

1. ✅ **تشغيل التطبيق**: `flutter run`
2. ✅ **اختبار تسجيل الدخول**
3. ✅ **استكشاف الواجهات**
4. ✅ **التطوير والتحسين**

### لاحقاً:

1. ⏳ **إكمال خدمات Firebase** (للوظائف الكاملة)
2. ⏳ **إضافة المزيد من الوظائف**
3. ⏳ **تحسين الواجهات**

---

## 🎯 ملخص:

- ✅ **الوضع المحلي مفعّل حالياً**
- ✅ **يمكنك البدء فوراً بدون Firebase**
- ✅ **البيانات تُحفظ محلياً**
- ✅ **سهل الانتقال لـ Firebase لاحقاً**

**شغّل التطبيق الآن واستمتع بالتطوير!** 🚀

---

## 📝 ملاحظات:

- الوضع المحلي مناسب للتطوير والاختبار
- للاستخدام الكامل، ستحتاج Firebase
- البيانات المحلية تبقى حتى تحذف التطبيق
- يمكن التبديل بين الوضعين بسهولة

