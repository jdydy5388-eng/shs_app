# دليل البدء السريع - بعد تحديث .env

## الخطوات التالية:

### 1. تثبيت التبعيات (Dependencies)

```bash
cd server
dart pub get
```

هذا الأمر سيقوم بتحميل جميع المكتبات المطلوبة:
- shelf (HTTP Server)
- postgres (PostgreSQL Driver)
- وغيرها من المكتبات

---

### 2. التحقق من إعدادات قاعدة البيانات

تأكد من أن ملف `.env` يحتوي على:

```env
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=shs_app
DATABASE_USER=postgres
DATABASE_PASSWORD=your_actual_password  # ⚠️ غيّر هذا!
```

**مهم جداً**: 
- تأكد من أن قاعدة البيانات `shs_app` موجودة في PostgreSQL
- تأكد من أن كلمة المرور صحيحة
- تأكد من أن PostgreSQL يعمل

---

### 3. تشغيل الخادم

```bash
cd server
dart run lib/main.dart
```

**ما يجب أن تراه:**

```
✅ Connected to database: shs_app
✅ Database tables created/verified
✅ Server running on http://0.0.0.0:8080
✅ API available at http://0.0.0.0:8080/api
```

إذا ظهرت رسائل خطأ:
- **خطأ الاتصال بقاعدة البيانات**: تحقق من إعدادات `.env` وأن PostgreSQL يعمل
- **خطأ المنفذ مستخدم**: غيّر `SERVER_PORT` في `.env`

---

### 4. اختبار الخادم

افتح المتصفح واذهب إلى:
```
http://localhost:8080/health
```

يجب أن ترى:
```
Server is running
```

---

### 5. إعداد التطبيق للاتصال بالخادم

في `lib/config/app_config.dart`:

```dart
class AppConfig {
  // غيّر إلى false للوضع الشبكي
  static const bool useLocalMode = false;
  
  // غيّر IP إلى IP الخادم (اكتشفه باستخدام ipconfig)
  static const String serverBaseUrl = 'http://192.168.1.100:8080';
  static const String apiBaseUrl = '$serverBaseUrl/api';
}
```

**اكتشاف IP الخادم:**
```powershell
ipconfig
# ابحث عن IPv4 Address في Ethernet adapter أو Wireless adapter
```

---

### 6. تشغيل التطبيق

```bash
# من المجلد الرئيسي (d:\shs_app)
flutter run
```

---

## استكشاف الأخطاء

### خطأ: "Failed to connect to database"

**الحل:**
1. تأكد من أن PostgreSQL يعمل:
   ```powershell
   # Windows
   Get-Service postgresql*
   ```
2. تحقق من إعدادات `.env`
3. جرّب الاتصال يدوياً:
   ```bash
   psql -U postgres -d shs_app
   ```

### خطأ: "Address already in use"

**الحل:**
- غيّر `SERVER_PORT` في `.env` إلى منفذ آخر (مثل 8081)
- أو أوقف البرنامج الذي يستخدم المنفذ 8080

### خطأ: "Database tables not created"

**الحل:**
- الخادم ينشئ الجداول تلقائياً عند أول تشغيل
- إذا لم تظهر، تحقق من صلاحيات المستخدم في PostgreSQL
- تأكد من أن قاعدة البيانات `shs_app` موجودة

---

## الأوامر السريعة

```powershell
# الانتقال إلى مجلد الخادم
cd server

# تثبيت التبعيات
dart pub get

# تشغيل الخادم
dart run lib/main.dart

# إيقاف الخادم
# اضغط Ctrl+C
```

---

## الخطوات الكاملة (ملخص):

1. ✅ تحديث `.env` (تم)
2. ⏳ تثبيت التبعيات: `dart pub get`
3. ⏳ تشغيل الخادم: `dart run lib/main.dart`
4. ⏳ اختبار الخادم: `http://localhost:8080/health`
5. ⏳ تحديث `app_config.dart` في التطبيق
6. ⏳ تشغيل التطبيق

---

## ملاحظات مهمة:

- **الخادم يجب أن يعمل دائماً** عندما تريد استخدام التطبيق في الوضع الشبكي
- **للإيقاف**: اضغط `Ctrl+C` في Terminal
- **للبدء التلقائي**: يمكنك إنشاء ملف `.bat` أو استخدام task scheduler

---

جاهز للبدء! 🚀

