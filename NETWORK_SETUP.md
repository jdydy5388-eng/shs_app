# دليل إعداد الشبكة - ربط الأجهزة

## المشكلة

عند إنشاء حساب على جهاز Windows، لا يتم التعرف عليه على الجوال لأن:
- الحساب كان يُحفظ محلياً في `SharedPreferences` فقط
- لم يكن يتم حفظه في الخادم المركزي

## الحل

تم تحديث `AuthProviderLocal` لاستخدام `DataService` في الوضع الشبكي، مما يعني:
- ✅ إنشاء الحساب يتم في الخادم
- ✅ تسجيل الدخول يتم من الخادم
- ✅ جميع الأجهزة في نفس الشبكة يمكنها الوصول لنفس الحسابات

## خطوات الإعداد

### 1. التأكد من أن الخادم يعمل

```bash
cd server
dart run lib/main.dart
```

يجب أن ترى:
```
✅ Server is running!
📍 Address: http://0.0.0.0:8080
```

### 2. معرفة IP جهاز الخادم (Windows)

```bash
ipconfig
```

ابحث عن `IPv4 Address` - مثال: `192.168.43.196`

### 3. تحديث إعدادات التطبيق على كل جهاز

في `lib/config/app_config.dart`:

**على جهاز Windows (الخادم):**
```dart
static const String serverBaseUrl = 'http://localhost:8080';
// أو
static const String serverBaseUrl = 'http://127.0.0.1:8080';
```

**على الجوال (أو أي جهاز آخر):**
```dart
static const String serverBaseUrl = 'http://192.168.43.196:8080';
// استبدل 192.168.43.196 بـ IP جهاز Windows
```

### 4. التأكد من أن الوضع الشبكي مفعّل

في `lib/config/app_config.dart` على **جميع الأجهزة**:
```dart
static const bool useLocalMode = false; // يجب أن يكون false
```

### 5. اختبار الاتصال

1. على جهاز Windows:
   - شغّل الخادم
   - شغّل التطبيق
   - أنشئ حساب جديد

2. على الجوال:
   - تأكد من أن IP الخادم صحيح
   - تأكد من أن `useLocalMode = false`
   - سجّل الدخول بنفس الحساب

### 6. حل المشاكل الشائعة

#### المشكلة: لا يمكن الاتصال بالخادم

**الحل:**
1. تأكد أن الخادم يعمل
2. تأكد من IP الصحيح
3. تأكد من أن الجوال والكمبيوتر في نفس الشبكة (WiFi)
4. تأكد من أن Firewall يسمح بالاتصال على المنفذ 8080

#### المشكلة: "Connection refused"

**الحل:**
- الخادم يعمل على `localhost` فقط - يجب أن يعمل على `0.0.0.0` (وهذا الافتراضي)
- تحقق من `server/lib/config/server_config.dart` - يجب أن يكون `SERVER_HOST=0.0.0.0`

#### المشكلة: الحساب موجود لكن لا يمكن تسجيل الدخول

**الحل:**
- تأكد من استخدام نفس كلمة المرور
- تأكد من أن الحساب موجود في قاعدة البيانات:
  ```sql
  SELECT * FROM users WHERE email = 'your@email.com';
  ```

## ملاحظات مهمة

1. **الوضع المحلي vs الشبكي:**
   - `useLocalMode = true` → البيانات محلية فقط (SQLite)
   - `useLocalMode = false` → البيانات مشتركة على الخادم (PostgreSQL)

2. **المصادقة البيومترية:**
   - يتم حفظ معلومات المستخدم محلياً على كل جهاز للمصادقة البيومترية
   - لكن المصادقة الأساسية (email/password) تتم من الخادم

3. **الأمان:**
   - في الإنتاج، يجب استخدام HTTPS
   - يجب hash كلمات المرور بشكل أفضل (حالياً يستخدم SHA-256)

## بعد التحديث

الآن عندما:
- ✅ تنشئ حساب على Windows → يُحفظ في الخادم
- ✅ تسجّل الدخول على الجوال → يبحث في الخادم
- ✅ جميع الأجهزة في نفس الشبكة → يمكنها الوصول لنفس الحسابات
