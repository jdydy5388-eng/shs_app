# إعداد Android USB Debugging لتشغيل Flutter على الجوال

## 🚨 المشكلة الحالية:
- ADB لا يتعرف على جهاز Android المتصل عبر USB
- `adb devices` لا يُظهر أي أجهزة

---

## ✅ الحلول (جرب بالترتيب):

---

### **الخطوة 1: تفعيل USB Debugging على جهاز Android** ⭐ (الأهم)

#### على جهاز Android:

1. **افتح "الإعدادات" (Settings)**
2. **ابحث عن "حول الهاتف" (About phone)** أو **"About device"**
3. **اضغط 7 مرات على "رقم الإصدار" (Build number)** أو **"رقم التجميع"**
   - سترى رسالة "أنت الآن مطور" (You are now a developer)
4. **ارجع إلى الإعدادات الرئيسية**
5. **ابحث عن "خيارات المطور" (Developer options)** أو **"Developer options"**
6. **افتح "خيارات المطور"**
7. **فعّل "تصحيح أخطاء USB" (USB debugging)** أو **"USB debugging"**
   - قد يظهر تحذير أمان - اضغط "موافق" (OK)

---

### **الخطوة 2: تفعيل USB Debugging على الكمبيوتر**

#### عند توصيل الجوال عبر USB:

1. **عند توصيل الجوال، سيظهر لك خيار "اتصال USB بالكمبيوتر"**
2. **اختر "نقل الملفات" (File Transfer - MTP)** أو **"USB debugging"**
3. **في المرة الأولى، سيظهر لك على شاشة الجوال:**
   - "السماح بتصحيح أخطاء USB؟" (Allow USB debugging?)
   - **اضغط "موافق" (OK)** أو **"Always allow from this computer"**
   - **اضغط "موافق" مرة أخرى**

---

### **الخطوة 3: التحقق من الاتصال**

#### في Terminal (PowerShell):

```powershell
# إعادة تشغيل ADB
adb kill-server
adb start-server

# التحقق من الأجهزة المتصلة
adb devices
```

**يجب أن ترى:**
```
List of devices attached
ABCD1234    device
```

**إذا رأيت:**
```
ABCD1234    unauthorized
```
**يعني:** يجب الموافقة على USB debugging على شاشة الجوال

---

### **الخطوة 4: تشغيل Flutter على الجوال**

```bash
# عرض الأجهزة المتاحة
flutter devices

# تشغيل التطبيق على الجوال
flutter run -d <device-id>
```

أو:
```bash
# إذا كان هناك جهاز واحد فقط
flutter run
```

---

## 🔧 حلول إضافية إذا استمرت المشكلة:

### **الحل 1: إعادة تثبيت ADB Drivers**

1. **افتح "Device Manager" في Windows**
2. **ابحث عن جهاز Android** (قد يظهر كـ "Unknown device" أو "Android Phone")
3. **اضغط كليك يمين → Update driver**
4. **اختر "Browse my computer for drivers"**
5. **أدخل مسار Android SDK:**
   ```
   C:\Users\USER\AppData\Local\Android\sdk\extras\google\usb_driver
   ```

---

### **الحل 2: استخدام USB 2.0 Port**

- بعض منافذ USB 3.0 قد تسبب مشاكل
- جرب توصيل الجوال في منفذ USB 2.0

---

### **الحل 3: تفعيل Wireless Debugging (Android 11+)**

إذا كان لديك Android 11 أو أحدث:

1. **في "خيارات المطور" → فعّل "Wireless debugging"**
2. **اضغط على "Wireless debugging"** للدخول
3. **اضغط "Pair device with pairing code"**
4. **في Terminal:**
   ```bash
   adb pair <ip-address>:<port>
   ```
5. **أدخل رمز الاقتران** (pairing code)
6. **ثم:**
   ```bash
   adb connect <ip-address>:<port>
   ```

---

### **الحل 4: إصلاح Android SDK Tools**

إذا ظهرت رسالة "cmdline-tools component is missing":

1. **افتح Android Studio**
2. **Tools → SDK Manager**
3. **في تبويب "SDK Tools"**
4. **فعّل "Android SDK Command-line Tools"**
5. **اضغط "Apply"**

أو يدوياً:
```powershell
# تحميل Command-line Tools
# من: https://developer.android.com/studio#command-line-tools-only
# ثم استخراجها إلى:
C:\Users\USER\AppData\Local\Android\sdk\cmdline-tools\
```

---

## 📋 خطوات سريعة للتشخيص:

```powershell
# 1. التحقق من ADB
adb --version

# 2. إعادة تشغيل ADB
adb kill-server
adb start-server

# 3. التحقق من الأجهزة
adb devices

# 4. التحقق من Flutter
flutter devices

# 5. تشغيل على الجوال
flutter run
```

---

## ✅ بعد التفعيل:

- ✅ جهاز Android سيظهر في `flutter devices`
- ✅ يمكنك تشغيل التطبيق باستخدام `flutter run`
- ✅ التطبيق سيُفتح تلقائياً على الجوال

---

## 💡 نصائح:

1. **في المرة الأولى:** قد تحتاج الموافقة على USB debugging على شاشة الجوال
2. **إذا لم يعمل:** جرب كبل USB آخر أو منفذ USB آخر
3. **للتأكد:** تحقق من أن "خيارات المطور" مفعّلة والجهاز في وضع "USB debugging"

---

**بعد تفعيل USB debugging على الجوال، جرب الأمر:**
```bash
flutter devices
```

يجب أن ترى جهاز Android الخاص بك! 🚀

